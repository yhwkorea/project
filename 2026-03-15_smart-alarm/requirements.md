# requirements.md — 도어-투-도어 알람 (스마트 교통 알람 Android 앱)

**작성일**: 2026-03-15
**작성자**: requirements-analyst
**상태**: UPDATED — 2026-03-16 (3차) — Google Maps Directions API 연동 요구사항 추가

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [핵심 사용자 시나리오](#2-핵심-사용자-시나리오)
3. [기능 요구사항](#3-기능-요구사항)
4. [비기능 요구사항](#4-비기능-요구사항)
5. [외부 API 후보](#5-외부-api-후보)
6. [기술 스택 제안](#6-기술-스택-제안)
7. [제약 조건](#7-제약-조건)
8. [미결 사항](#8-미결-사항)
9. [결정 이력](#9-결정-이력)
10. [Google Maps Directions API 연동 — 신규 기능 요구사항](#10-google-maps-directions-api-연동--신규-기능-요구사항)

---

## 1. 프로젝트 개요

### 1.1 문제 정의

대중교통 이용자는 매일 아침 "몇 시에 일어나야 하지?"를 고민한다. 기존 알람 앱은 고정된 시각에 울리기 때문에, 당일 버스 지연이나 교통 체증이 반영되지 않는다. 결과적으로 너무 일찍 일어나거나 지각하는 상황이 반복된다.

**핵심 통찰**: 집 문 → 회사 문까지의 도어-투-도어(D2D) 이동 시간은 매일 달라진다. 이 변동성을 실시간으로 추적하고, 역산(逆算)하여 사용자가 출발해야 할 시각을 자동으로 알려주는 앱이 필요하다.

### 1.2 제품 비전

> "목적지 도착 시각을 입력하면, 앱이 실시간 교통 상황을 분석해 최적 기상 시각과 출발 시각을 알림으로 제공한다."

### 1.3 타겟 사용자

- 매일 정해진 시각까지 출근/등교해야 하는 대한민국 대중교통 이용자
- 버스 + 지하철을 복합적으로 이용하는 환승 사용자 포함

### 1.4 범위

- **플랫폼**: Android (한국 내 서비스)
- **교통수단**: 버스, 지하철 (도보 구간 포함)
- **지역**: 서울시 관할 대중교통 (MVP 기준) — 전국 확대는 2차 로드맵

---

## 2. 핵심 사용자 시나리오

### 시나리오 A — 일반 출근 (정상 상황)

```
[사전 설정]
- 출발지(집): 서울 마포구 홍대입구역 인근 (GPS 또는 주소)
- 목적지: 강남구 역삼동 회사 (주소)
- 도착 목표 시각: 09:00
- 준비 시간(버퍼): 30분 (씻기 + 아침 식사)
- 여유시간(bufferMinutes): 5분
- 모니터링 시작: 07:00

[당일 흐름]
07:00  앱이 백그라운드에서 교통 모니터링 시작
07:15  현재 D2D 소요 예상: 55분 → 출발 목표 08:05
        → "07:30에 일어나세요" 알림 (준비 30분 + 여유 5분 포함)
07:30  기상 알람 울림
08:00  앱 재확인: 2호선 지연 5분 감지 → D2D 소요 변경 65분
        → "출발 시각이 07:55로 앞당겨졌습니다" 재알림 (이미 늦음 경고)
08:05  사용자 출발 (지연 흡수 가능 여부 계산 후 안내)
```

### 시나리오 B — 버스 지연 + 재알림

```
07:40  기상 알람 울림 (정상 계산: D2D 50분 → 출발 08:10)
08:00  앱 체크: 타야 할 버스 15분 지연 감지
        → "버스 지연으로 출발 시각이 07:58로 변경됐습니다.
           지금 나가야 합니다!" 긴급 알림
08:02  앱: 지연된 버스 대신 지하철 환승 경로 대안 제시
        → "지하철 경로로 바꾸면 09:02 도착 예정입니다"
```

### 시나리오 C — 우천 / 교통 체증 반영

```
전날 밤 11:00  내일 오전 우천 예보 감지
               → "내일 우천으로 이동 시간 +10분 예상. 알람을 10분 앞당길까요?" 알림
사용자: [확인]  → 알람 자동 조정
당일 07:20     실제 교통 상황 기반 최종 확인 후 알람 보정
```

### 시나리오 D — 초기 설정 플로우

```
앱 설치 후 온보딩:
1. 출발지 설정 (현재 위치 사용 or 주소 검색)
2. 목적지 추가 (복수 목적지 지원 — 직장, 학교 등)
3. 각 목적지별 도착 목표 시각 설정
4. 준비 시간 설정 (슬라이더: 0~90분)
5. 여유시간 설정 (슬라이더: 0~30분, 기본값 5분)
6. 모니터링 시작 시각 설정
7. 알림 방식 선택 (소리, 진동, 팝업)
```

---

## 3. 기능 요구사항

### 3.1 설정 관리

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-001 | 사용자는 출발지(집)를 GPS 현재 위치 또는 텍스트 주소 입력으로 설정할 수 있다. 텍스트 입력은 한국어·영어 모두 지원하며, 영어 역명 조회 실패 시 "역명 Seoul Korea" 형식으로 fallback 재시도한다. | MUST |
| FR-002 | 사용자는 목적지를 1개 이상 등록할 수 있다. (복수 등록 지원) | MUST |
| FR-003 | 각 목적지별로 도착 목표 시각을 설정할 수 있다. | MUST |
| FR-004 | 준비 시간(preparationMinutes)을 분 단위로 설정할 수 있다. (슬라이더 범위: 0~90분, 기본값: 30분) | MUST |
| FR-004b | 여유시간(bufferMinutes)을 분 단위로 설정할 수 있다. (슬라이더 범위: 0~30분, 기본값: 5분) 기상시각 = 도착 목표 시각 - 경로 소요시간 - 준비시간 - 여유시간 | MUST |
| FR-005 | 교통 모니터링 시작 시각을 설정할 수 있다. (슬라이더 범위: 0분 이상) | MUST |
| FR-006 | 요일별로 알람 활성화/비활성화를 설정할 수 있다. (월~일 개별 설정) | SHOULD |

### 3.2 경로 계산

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-010 | 출발지 → 목적지 간 버스·지하철·도보를 조합한 복합 경로를 계산한다. | MUST |
| FR-011 | 환승 정보(환승 횟수, 환승 대기 시간)를 포함한 경로를 제공한다. | MUST |
| FR-012 | 도보 구간 거리 및 소요 시간을 경로 계산에 포함한다. | MUST |
| FR-013 | 복수 경로 후보를 제시하고, 현재 교통 상황 기준 최적 경로를 추천한다. ODsay 응답의 최대 5개 경로를 탭(Tab) UI로 선택 가능하며, 탭마다 총 소요 시간·도보 시간·환승 횟수를 표시한다. | MUST |
| FR-013b | **경로 확인 필수화**: 알람 저장 전 반드시 경로를 선택해야 한다. 경로 선택 없이는 저장 버튼이 비활성화된다. | MUST |
| FR-014 | D2D(도어-투-도어) 총 소요 시간을 계산한다: 준비시간 + 여유시간 + 도보 + 대기 + 이동 | MUST |
| FR-015 | 특정 경로의 실시간 소요 시간을 N분 간격으로 재계산한다. | MUST |
| FR-016 | 경로 미리보기 카드의 "지도 보기" 버튼을 누르면 선택한 경로의 출발지·목적지를 Google Maps WebView로 표시한다. | SHOULD |
| FR-016b | **지금 출발 시 도착 예정 표시**: 경로 미리보기 카드에 현재 시각 기준으로 "지금 출발하면 XX:XX 도착 예정" 정보를 표시한다. | SHOULD |
| FR-017 | ODsay API가 경로 없음(-8), 서비스 시간 외(-98) 등 에러 코드를 반환할 경우, 사용자에게 "이 시간대에 운행하는 대중교통이 없어요" 등 에러 코드에 맞는 명확한 안내 메시지를 표시한다. (새벽 운행 중단 안내 포함) | MUST |
| FR-018 | **교통 운행 시간 필터**: 새벽 00:30~05:30 대중교통 운행 중단 시간대에는 대중교통 경로를 제외하고 도보(WALK) 경로만 표시한다. | MUST |

### 3.3 실시간 교통 정보

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-020 | 버스 실시간 도착 정보(잔여 정류장, 예상 도착 시각)를 조회한다. | MUST |
| FR-021 | 지하철 실시간 운행 정보(지연, 혼잡도)를 조회한다. | MUST |
| FR-022 | 교통 정체 정보(도로 소통 상황)를 조회하여 도보 경로 우회 등에 반영한다. | SHOULD |
| FR-023 | **날씨 API 연동**: OpenWeatherMap Current Weather API를 사용하여 실시간 기상 상태를 조회하고, 모니터링 시 이동시간에 배수를 적용한다. 적용 기준: 비(Rain) +15%, 눈(Snow) +25%, 뇌우(Thunderstorm) +20%. | MUST |

### 3.4 알림 시스템

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-030 | 계산된 기상 시각(도착 목표 - D2D 소요시간 - 준비시간 - 여유시간)에 알람을 발생시킨다. | MUST |
| FR-031 | 모니터링 중 교통 상황 변화로 출발 시각이 변경되면 재알림을 발송한다. | MUST |
| FR-032 | 재알림은 변경된 출발 시각, 변경 원인(버스 지연 N분, 지하철 운행 장애 등)을 포함한다. | MUST |
| FR-033 | 우천 등 기상 변화 예보 시 전날 또는 당일 이른 시각에 사전 알림을 발송한다. | SHOULD |
| FR-034 | 이미 지나간 최적 출발 시각에 대해 "N분 지남" 경고 알림을 제공한다. | SHOULD |
| FR-035 | 알림음, 진동 패턴, 팝업 스타일을 사용자가 커스터마이징할 수 있다. | COULD |
| FR-036 | 알람을 스누즈할 경우, 스누즈 시간(5분 기본) 후 재알림 시 교통 상황을 재계산하여 경고한다. | SHOULD |

### 3.5 실시간 모니터링 스케줄링

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-037 | **모니터링 WorkManager 전략**: TrafficMonitorWorker는 상시 15분 주기 PeriodicWork 대신 OneTimeWorkRequest 방식으로 운영한다. | MUST |
| FR-037b | 모니터링 시작 시각(도착 목표 시각 - monitoringStartMinutes)에 WorkRequest를 1회 예약하고, 이후 3분마다 재실행한다(도착 목표 시각 전까지). | MUST |
| FR-037c | AlarmReceiver 발화 후 다음 날 같은 요일 조건이 활성화되어 있으면 다음 모니터링 WorkRequest를 자동으로 재예약한다. | MUST |

### 3.6 기상 시각 계산 (CalculateWakeTimeUseCase)

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-038 | **과거 기상 시각 처리**: 계산된 기상 시각이 현재 시각보다 과거인 경우, 다음으로 활성화된 요일(활성 요일 순환 탐색)의 기상 시각을 계산한다. | MUST |
| FR-038b | **모니터링 중 과거 기상 시각 처리**: 모니터링 실행 중 계산된 기상 시각이 이미 과거인 경우, 즉시 알람을 발화한다 (+3초 딜레이). | MUST |
| FR-038c | **previewWakeMinutes 즉시 재계산**: 도착 목표 시각, 준비시간, 여유시간(bufferMinutes) 중 하나라도 변경되면 previewWakeMinutes(예상 기상 시각 미리보기)를 즉시 재계산하여 UI에 반영한다. | MUST |

### 3.7 대안 경로 제안

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-040 | 현재 경로에 지연/장애 발생 시 대안 경로를 자동으로 계산하여 제시한다. | MUST |
| FR-041 | 대안 경로와 원래 경로의 예상 도착 시각 차이를 비교하여 보여준다. | MUST |
| FR-042 | 사용자가 대안 경로를 선택하면 해당 경로 기준으로 모니터링을 전환한다. | SHOULD |

### 3.8 히스토리 및 분석

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-050 | 최근 30일간 D2D 소요 시간 이력을 저장하고, 요일별 평균을 표시한다. | COULD |
| FR-051 | 예측 도착 시각과 실제 도착 시각의 오차를 기록하여 알고리즘 보정에 활용한다. | COULD |

---

## 4. 비기능 요구사항

### 4.1 성능

| ID | 요구사항 |
|----|----------|
| NFR-001 | 경로 계산 응답 시간: 최초 계산 3초 이내, 재계산(갱신) 1.5초 이내 |
| NFR-002 | 실시간 교통 정보 갱신 주기: 모니터링 시작 후 3분 간격 (교통 이상 감지 시 1분 간격으로 전환) |
| NFR-003 | 앱 콜드 스타트(초기 실행) 2초 이내 |

### 4.2 신뢰성

| ID | 요구사항 |
|----|----------|
| NFR-010 | 외부 API 응답 실패 시 캐시된 마지막 데이터로 폴백하고, 사용자에게 "실시간 정보 없음" 표시 |
| NFR-011 | 네트워크 오프라인 상태에서도 가장 최근 계산된 알람은 유지되어야 함 |
| NFR-012 | 백그라운드 서비스가 Android 시스템에 의해 종료될 경우, WorkManager로 자동 재시작 |
| NFR-013 | 알람 발생 정확도: 목표 시각 기준 ±1분 이내 |

### 4.3 배터리 및 리소스

| ID | 요구사항 |
|----|----------|
| NFR-020 | 백그라운드 실행 시 배터리 소모를 최소화한다. (Doze 모드 대응 필수) |
| NFR-021 | 모니터링 비활성화 시간대(모니터링 시작 시각 이전)에는 네트워크 요청을 하지 않는다. |
| NFR-022 | API 호출 횟수를 최소화하기 위해 응답 캐싱 전략을 적용한다. |

### 4.4 사용성

| ID | 요구사항 |
|----|----------|
| NFR-030 | 초기 설정은 5단계 이내의 온보딩으로 완료 가능해야 한다. |
| NFR-031 | 알림 내용은 현재 상황(지연 원인, 권장 행동)을 1~2줄로 명확하게 전달해야 한다. |
| NFR-032 | Material Design 3 가이드라인을 준수한다. |

### 4.5 보안 및 개인정보

| ID | 요구사항 |
|----|----------|
| NFR-040 | 위치 정보는 기기 내에 암호화하여 저장하고, 서버에 전송하지 않는다. (API 호출 시 최소한의 좌표만 전송) |
| NFR-041 | API 키는 앱 번들에 평문 하드코딩하지 않는다. (빌드 환경변수 또는 난독화 처리) |
| NFR-042 | Android 위치 권한(정밀 위치)은 명시적 동의 후 취득한다. |
| NFR-043 | 개인정보 처리방침 화면을 앱 내 제공한다. |

---

## 5. 외부 API 후보

### 5.1 대중교통 경로 계산

| API | 제공사 | 장점 | 단점 | 비용 |
|-----|--------|------|------|------|
| **Tmap 대중교통 API** | SK텔레콤 | 실시간 도착정보 + 환승 + 도보 통합, 국내 커버리지 우수 | 월 호출량 제한(무료 100만), 상업용 요금제 별도 | 무료(제한) / 유료 |
| **카카오 모빌리티 API** | 카카오 | 경로 품질 우수, 카카오맵 수준 정확도, 대중교통+도보 통합 | 상업용 사용 시 비용 발생, 도착정보 실시간성 제한 | 무료(제한) / 유료 |
| **네이버 지도 API** | 네이버 | 국내 사용자 친숙도 높음, 대중교통 경로 품질 우수 | 모바일 앱 직접 호출 제한(웹 중심), 상업 이용 조건 복잡 | 무료(제한) / 유료 |
| **공공데이터포털 TAGO** | 국토교통부 | 완전 무료, 전국 버스 노선/도착 정보 | 지하철 포함 안 됨, API 안정성 낮음, 응답 느림 | 무료 |

**추천**: **ODsay Lab API (주 API)** — 일 1,000회 무료, 대중교통 경로+지하철 실시간 통합, Tmap은 보조 API로 운용 (Tmap 상업 이용 계약 불가 → TBD-004 결정)

### 5.2 버스 실시간 도착 정보

| API | 제공사 | 장점 | 단점 | 비용 |
|-----|--------|------|------|------|
| **서울 버스 API** | 서울시 공공데이터 | 무료, 서울 버스 실시간 도착 정확 | 서울 한정 | 무료 |
| **경기 버스 API** | 경기도 공공데이터 | 무료, 경기도 버스 커버 | 경기도 한정, 파편화됨 | 무료 |
| **전국 버스 TAGO** | 국토교통부 | 전국 커버 | 안정성 낮음, 지역별 데이터 품질 편차 | 무료 |

**추천**: **ODsay Lab API 통해 통합 호출** — 서울시 버스 실시간 도착 포함 (서울시 공공데이터 API는 보조로 직접 연동 가능)

### 5.3 지하철 실시간 운행 정보

| API | 제공사 | 장점 | 단점 | 비용 |
|-----|--------|------|------|------|
| **서울 지하철 실시간 API** | 서울열린데이터광장 | 무료, 서울 1~9호선 + 경의중앙선 등 실시간 위치 | 서울 한정, 갱신 주기 30초~1분 | 무료 |
| **코레일 API** | 한국철도공사 | KTX/광역철도 운행 정보 | 도시철도 미포함, 실시간성 제한 | 무료 |
| **ODsay Lab API** | ODsay | 대중교통 경로 + 지하철 실시간 통합, 안정성 높음 | 유료(소규모 무료 플랜) | 무료(일 1,000회) / 유료 |

**추천**: **ODsay Lab API (주)** + 서울열린데이터광장 실시간 위치 API (보조 직접 호출) — MVP 서울 한정이므로 이 조합으로 충분

### 5.4 기상 정보

| API | 제공사 | 장점 | 단점 | 비용 |
|-----|--------|------|------|------|
| **기상청 단기예보 API** | 기상청 | 무료, 3시간 단위 예보, 강수 확률 포함 | 갱신 주기 3시간, 실시간성 낮음 | 무료 |
| **기상청 초단기실황 API** | 기상청 | 무료, 1시간 이내 실황 | 1km 격자 단위 정밀도 | 무료 |
| **OpenWeatherMap Current Weather API** | OpenWeather | 전세계 커버, 호출 간편, 날씨 상태 코드(Rain/Snow/Thunderstorm) 직접 제공 → 이동시간 배수 적용에 적합 | 한국 정밀도 기상청 대비 낮음 | 무료(제한) / 유료 |

**추천**: **OpenWeatherMap Current Weather API** — 실시간 날씨 상태 코드(비/눈/뇌우) 기반으로 이동시간 배수(+15%/+25%/+20%)를 적용하는 FR-023에 최적. 예보 기반 사전 알림(FR-033)은 기상청 단기예보 API 보조 활용.

### 5.5 교통 혼잡도

| API | 제공사 | 장점 | 단점 | 비용 |
|-----|--------|------|------|------|
| **Tmap 교통정보 API** | SK텔레콤 | 도로 소통 정보, 대중교통 경로와 동일 제공사 | 유료 상위 플랜 필요 | 유료 |
| **카카오 모빌리티** | 카카오 | 도로 혼잡도 실시간 | 대중교통 직접 연동 안 됨 | 유료 |

**추천**: MVP에서는 기상청 데이터로 간접 반영 (우천 계수 방식, TBD-005 결정) — Tmap 교통정보는 2차 고도화 대상

---

## 6. 기술 스택 제안

### 6.1 플랫폼

| 항목 | 선택 | 이유 |
|------|------|------|
| 언어 | Kotlin | Android 공식 권장, Coroutine 지원으로 비동기 API 호출 처리 용이 |
| 최소 SDK | API 26 (Android 8.0 Oreo) | WorkManager + 알림 채널 안정 지원, 2026년 기준 국내 95%+ 커버 |
| 타겟 SDK | API 35 (Android 15) | 최신 배터리/백그라운드 정책 대응 |
| UI | Jetpack Compose | 선언형 UI, Material Design 3 네이티브 지원 |

### 6.2 아키텍처

```
UI Layer (Compose)
    ↕
ViewModel (StateFlow)
    ↕
Repository Layer
    ↕
Data Sources
  ├── Remote (Retrofit + OkHttp → 외부 API)
  ├── Local (Room DB → 설정, 경로 캐시, 히스토리)
  └── WorkManager (백그라운드 모니터링 작업 — OneTimeWorkRequest 방식)
```

- 패턴: MVVM + Clean Architecture (UseCase 레이어 포함)
- 의존성 주입: Hilt
- 비동기: Kotlin Coroutines + Flow

### 6.3 주요 라이브러리

| 범주 | 라이브러리 |
|------|-----------|
| 네트워크 | Retrofit2 + OkHttp3 + kotlinx.serialization |
| 로컬 DB | Room |
| 백그라운드 | WorkManager (OneTimeWorkRequest 기반 모니터링) |
| 위치 | Google Play Services Location (FusedLocationProvider) |
| 알림 | Android Notification API (NotificationCompat) |
| 의존성 주입 | Hilt |
| 지도 | Android WebView + Google Maps URL (FR-016 경로 지도 보기); Naver/Kakao Map SDK는 2차 고도화 대상 |
| 테스트 | JUnit5 + MockK + Turbine (Flow 테스트) |

---

## 7. 제약 조건

| 구분 | 내용 |
|------|------|
| 지역 | **서울시 관할 대중교통 전용 (MVP 기준)**. 부산·대구 등 전국 확대는 2차 로드맵 |
| 교통수단 | 버스·지하철·도보만 지원. 택시·자가용 미포함 (추후 확장 가능) |
| 운행 시간 | 새벽 00:30~05:30 대중교통 운행 중단 — 해당 시간대 경로 조회 시 WALK만 표시 |
| OS | Android 전용. iOS 미지원 (요구사항 명시) |
| 오프라인 | 실시간 교통 정보는 인터넷 연결 필수. 오프라인 시 캐시 데이터 폴백 |
| API 키 | **ODsay Lab (일 1,000회 무료)**, 서울열린데이터광장, OpenWeatherMap, 기상청 등 각 API 발급 필요. Tmap은 보조 API로만 활용 |
| 배터리 | Android 8.0+ 백그라운드 실행 제한 정책 준수 필수 (Doze, App Standby) |
| 알람 정확도 | Android 12+ 정확한 알람(SCHEDULE_EXACT_ALARM) 권한 별도 취득 필요 |
| 알림 방식 | **로컬 알림만** (WorkManager + AlarmManager). FCM 등 서버 푸시 없음 — 서버 없는 순수 클라이언트 앱 |
| 개인정보 | 위치 정보 수집 → 개인정보보호법 준수, 앱 내 개인정보 처리방침 필수 |

---

## 8. 미결 사항

> 구현 전 확인이 필요한 항목들. 이 항목들이 해소되어야 설계(threat-modeler) 단계로 진행 가능.

| ID | 항목 | 중요도 | 현재 가정 |
|----|------|--------|----------|
| TBD-001 | ~~서울 외 지역(부산, 대구 등) 지원 범위? 전국 or 서울/수도권 우선?~~ | HIGH | **RESOLVED**: 서울시 관할 대중교통만 (MVP 기준). 전국 확대는 2차 로드맵 |
| TBD-002 | 복수 목적지 지원 시 최대 개수? 우선순위 관리 방식? | MEDIUM | 최대 5개, 메인 목적지 1개 고정으로 가정 |
| TBD-003 | 도착 목표 시각이 지난 경우 자동 비활성화? 수동 비활성화? | MEDIUM | 도착 목표 시각 +30분 후 자동 비활성화 가정 |
| TBD-004 | ~~Tmap API 상업 이용 여부? (개인 프로젝트 vs 배포 앱)~~ | HIGH | **RESOLVED**: Tmap 상업 이용 계약 불가 → **ODsay Lab을 주 API**(일 1,000회 무료)로 전환. Tmap은 보조 API로만 활용 |
| TBD-005 | ~~기상 데이터 반영 방식: 우천 시 D2D 시간 증가 계수를 어떻게 산정?~~ | MEDIUM | **RESOLVED**: OpenWeatherMap Current Weather API 날씨 상태 코드 기반 — 비(Rain) +15%, 눈(Snow) +25%, 뇌우(Thunderstorm) +20% 배수 적용 (모니터링 시 적용) |
| TBD-006 | ~~실제 도착 시각 측정 방법? (목적지 GPS 도달 감지 여부)~~ | MEDIUM | **RESOLVED**: **GPS 체류 감지** 방식 — 사용자가 목적지 반경 내에 일정 시간 체류 시 자동 도착 감지. 서버 없는 순수 클라이언트 앱이므로 FCM 불가, FusedLocationProvider 기반 Geofencing 활용 |
| TBD-007 | 백그라운드 모니터링 주기(3분)가 배터리에 미치는 실측 영향? | HIGH | 프로토타입에서 실측 후 조정 |
| TBD-008 | 지하철 + 버스 혼용 경로에서 두 API의 시각 불일치 처리 방식? | HIGH | **UPDATED**: 경로 계산 API(**ODsay Lab**) 값을 주(primary), 서울열린데이터광장 실시간 API를 보조로 사용 (TBD-004 결정에 따라 Tmap → ODsay로 변경) |
| TBD-009 | ~~앱 내 지도 경로 시각화 필요 여부? (경로를 지도 위에 그리기)~~ | LOW | **RESOLVED**: FR-016으로 구현 — 경로 미리보기 카드에서 "지도 보기" 버튼 → Google Maps WebView로 출발지/목적지 표시 |
| TBD-010 | 다중 사용자 계정(가족 공유 등) 기능 필요 여부? | LOW | MVP 범위 외로 제외 |
| TBD-011 | Google Maps Directions API 사용 전략: ODsay 완전 대체 vs 병행? | HIGH | **RESOLVED**: 섹션 10 참고 — Hybrid 전략(Google 주, ODsay fallback) 채택 |
| TBD-012 | Google Maps Directions API 월 비용 초과 시 대응 방안? | HIGH | 섹션 10.6 참고 — ODsay fallback 자동 전환 + 사용량 모니터링 경고 |

---

---

## 9. 결정 이력

| 일자 | 항목 | 결정 내용 |
|------|------|----------|
| 2026-03-15 | TBD-001 지역 범위 | 서울시 관할 대중교통만 (MVP) |
| 2026-03-15 | TBD-004 주 API | Tmap 상업 이용 불가 → ODsay Lab(일 1,000회 무료)으로 전환, Tmap 보조 유지 |
| 2026-03-15 | TBD-005 우천 보정 | 강수량 단계별 계수 + 사용자 설정 조정 기능 |
| 2026-03-15 | TBD-006 도착 감지 | GPS 체류 감지(Geofencing) 방식 — 서버 없는 클라이언트 앱 |
| 2026-03-15 | 알림 방식 확정 | 로컬 알림만 (WorkManager + AlarmManager), FCM 없음 |
| 2026-03-16 | FR-001 수정 | 출발지 설정에 텍스트 주소 입력 추가 (한국어·영어), 영어 역명 fallback "역명 Seoul Korea" 형식 |
| 2026-03-16 | FR-013 수정 | 복수 경로 후보 탭 UI 구현 — ODsay 최대 5개 경로, 탭마다 총 시간/도보 시간/환승 횟수 표시 |
| 2026-03-16 | FR-016 신규 | 경로 지도 보기 — "지도 보기" 버튼 → Google Maps WebView (TBD-009 해소) |
| 2026-03-16 | FR-017 신규 | 새벽 운행 중단 안내 — ODsay 에러 코드(-8, -98 등) 기반 명확한 메시지 표시 |
| 2026-03-16 (2차) | FR-018 신규 | 교통 운행 시간 필터 — 00:30~05:30 대중교통 중단 시간대 경로 제외, WALK만 표시 |
| 2026-03-16 (2차) | FR-023 수정 | 날씨 API → OpenWeatherMap Current Weather API 확정. 비+15%, 눈+25%, 뇌우+20% 이동시간 배수 (TBD-005 보완) |
| 2026-03-16 (2차) | FR-004b 신규 | 여유시간(bufferMinutes) — 0~30분, 기본 5분. 기상시각 = 도착 - 경로 - 준비 - 여유 |
| 2026-03-16 (2차) | FR-004/FR-005 수정 | 준비시간·여유시간·교통확인 시작 슬라이더 range 하한 0으로 변경 |
| 2026-03-16 (2차) | FR-013b 신규 | 경로 확인 필수화 — 경로 선택 없이 알람 저장 불가 |
| 2026-03-16 (2차) | FR-016b 신규 | 지금 출발 시 도착 예정 — 경로 미리보기 카드에 현재 시각 기준 도착시간 표시 |
| 2026-03-16 (2차) | FR-037/037b/037c 신규 | 모니터링 스케줄링 — PeriodicWork 제거, OneTimeWork 기반. 도착 - monitoringStartMinutes 시각에 1회 예약, 3분마다 재실행, AlarmReceiver 후 다음 날 재예약 |
| 2026-03-16 (2차) | FR-038/038b/038c 신규 | CalculateWakeTimeUseCase — 과거 시각 시 다음 활성 요일 탐색, 모니터링 중 과거면 +3초 즉시 발화, bufferMinutes 반영한 previewWakeMinutes 즉시 재계산 |
| 2026-03-16 (2차) | NFR-002 수정 | 모니터링 갱신 주기 5분 → 3분으로 변경 (FR-037b 반영) |
| 2026-03-16 (3차) | TBD-011 해소 | Google Maps Directions API 연동 전략 확정 — Hybrid(Google 주, ODsay fallback). 섹션 10 신규 추가 |

---

## 10. Google Maps Directions API 연동 — 신규 기능 요구사항

> **작성일**: 2026-03-16 (3차)
> **작성자**: requirements-analyst
> **관련 기존 요구사항**: FR-010~FR-015, FR-017~FR-018, NFR-010, NFR-041

---

### 10.1 ODsay 완전 대체 vs 병행 사용 — 결정

**결론: Hybrid 전략 채택 — Google Directions를 주 실시간 소스, ODsay를 fallback으로 운용**

#### 비교 분석

| 기준 | ODsay Lab API | Google Maps Directions API |
|------|---------------|---------------------------|
| 실시간성 | 정적 스케줄 기반 (실시간 현황 제한) | 실제 버스/지하철 현황 반영 (`departure_time` 기반) |
| 한국 대중교통 커버리지 | 우수 (국내 전용, 서울 버스·지하철 통합) | 양호 (서울 주요 노선 지원, 일부 소규모 노선 누락 가능) |
| 비용 | 무료 (일 1,000회) | 월 $200 크레딧 (약 4만 건), 초과 시 1,000건당 $5 |
| 응답 포맷 | ODsay 고유 포맷 (한국어 노선 정보 풍부) | Google 표준 JSON (`legs[].steps[]`) |
| 새벽 운행 중단 처리 | 에러 코드(-8, -98) 반환 → FR-017 필터 | `departure_time` 범위 밖이면 응답 없거나 다른 날 시각 반환 → 앱 레벨 필터 여전히 필요 |
| 장애 리스크 | Google 대비 안정성 낮음, 소규모 서비스 | Google 인프라, SLA 보장, 전 세계 스케일 |

#### 채택 전략

```
[RouteRepository 호출 순서]
1순위: Google Maps Directions API (실시간 교통 현황 반영)
2순위: ODsay Lab API (Google 실패 시 자동 fallback)
3순위: 로컬 캐시 (두 API 모두 실패 시, NFR-010 준수)
```

**근거**:
- Google Directions는 실시간 버스/지하철 현황을 반영하여 기상 시각 계산 정확도를 높인다.
- ODsay는 완전 제거하지 않고 fallback으로 유지 → 비용 급증 방어 + API 장애 대응.
- 완전 대체 시 ODsay의 한국어 노선 정보(정류장명, 노선 코드 등)가 소실되어 FR-032 재알림 메시지 품질 저하 우려.

---

### 10.2 Google Directions API 응답 → RouteType 매핑

기존 `RouteType` enum(WALK / BUS / SUBWAY / BUS_SUBWAY)을 그대로 유지하며, Google API 응답 필드로 매핑한다.

#### 매핑 규칙

```
Google Directions API 응답 필드: legs[].steps[].travel_mode + transit_details.line.vehicle.type

travel_mode = "WALKING"
  → RouteType.WALK

travel_mode = "TRANSIT"
  + vehicle.type = "BUS"
  → RouteType.BUS

travel_mode = "TRANSIT"
  + vehicle.type IN ["SUBWAY", "HEAVY_RAIL", "COMMUTER_TRAIN", "METRO_RAIL"]
  → RouteType.SUBWAY

travel_mode = "TRANSIT" (복수 steps 내 BUS + SUBWAY 혼재)
  → RouteType.BUS_SUBWAY

매핑 불가 vehicle.type (FERRY, CABLE_CAR 등)
  → RouteType.WALK로 fallback + 로그 기록
```

#### 구현 위치

- 신규 클래스: `GoogleDirectionsMapper` — Google 응답 DTO → 도메인 `RouteInfo` + `RouteType` 변환 담당
- 기존 `OdsayRouteMapper`와 동일한 출력 타입(`RouteInfo`) 반환 → Repository 상위 레이어 변경 없음

---

### 10.3 API 키 관리

#### 요구사항 (NFR-041 확장)

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-060 | Google Maps Directions API 키는 `local.properties`에 `GOOGLE_MAPS_API_KEY=...` 형식으로 저장하고, `secrets-gradle-plugin`(com.google.android.libraries.mapsplatform.secrets-gradle-plugin)을 통해 `BuildConfig.GOOGLE_MAPS_API_KEY`로 주입한다. | MUST |
| FR-061 | `local.properties`는 `.gitignore`에 포함되어 Git에 커밋되지 않아야 한다. CI/CD 환경에서는 환경 변수로 주입한다. | MUST |
| FR-062 | Google Cloud Console에서 API 키에 Android 앱 제한(패키지명 + SHA-1 지문)을 설정하여 키 무단 사용을 방지한다. | MUST |
| FR-063 | Google Maps Directions API 사용량을 Google Cloud Monitoring에서 모니터링하고, 월 크레딧($200) 80% 도달 시 알림을 설정한다. | SHOULD |

---

### 10.4 실패 시 Fallback 전략

#### Fallback 흐름도

```
TrafficMonitorWorker / RouteRepository.getRoute()
    │
    ▼
[1] Google Maps Directions API 호출
    │
    ├─ 성공 → RouteInfo 반환 (실시간 현황 반영)
    │
    └─ 실패 (HTTP 4xx/5xx, timeout, quota 초과)
         │
         ▼
        [2] ODsay Lab API 호출 (기존 로직)
             │
             ├─ 성공 → RouteInfo 반환 + UI에 "실시간 정보 제한" 배지 표시
             │
             └─ 실패
                  │
                  ▼
                 [3] 로컬 캐시 (Room DB 최근 RouteInfo)
                      │
                      ├─ 캐시 있음 → RouteInfo 반환 + UI에 "캐시 데이터" 경고 표시 (NFR-010)
                      │
                      └─ 캐시 없음 → 사용자에게 "경로 조회 실패" 에러 표시, 알람 비활성화 안내
```

#### 요구사항

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-064 | Google Directions API 호출 실패 시 자동으로 ODsay API로 전환하고, 전환 여부를 UI에 표시한다. | MUST |
| FR-065 | Google API quota 초과(HTTP 429) 시 해당 모니터링 사이클에서 ODsay로 즉시 전환하며, 다음 사이클에서 Google API를 재시도한다. | MUST |
| FR-066 | fallback 발생 시 로그를 기록하여 추후 비용/장애 분석에 활용한다. | SHOULD |

---

### 10.5 기존 RouteRepositoryImpl 변경 범위

#### 현재 구조 (변경 전)

```
RouteRepositoryImpl
  └── OdsayDataSource (단일 데이터소스)
```

#### 변경 후 구조

```
RouteRepository (interface — 변경 없음)
    │
RouteRepositoryImpl (변경)
  ├── GoogleDirectionsDataSource (신규)  ← 1순위
  └── OdsayDataSource (기존 유지)         ← 2순위 fallback
```

#### 변경 범위 상세

| 구성요소 | 변경 유형 | 내용 |
|---------|----------|------|
| `RouteRepository` (interface) | 변경 없음 | 기존 계약 유지 |
| `RouteRepositoryImpl` | 수정 | `GoogleDirectionsDataSource` 주입 추가, fallback 로직 추가 (10~20줄 내외) |
| `OdsayDataSource` | 변경 없음 | fallback으로 그대로 유지 |
| `GoogleDirectionsDataSource` | 신규 추가 | Retrofit 인터페이스 + Google Directions REST API 호출 |
| `GoogleDirectionsMapper` | 신규 추가 | Google 응답 DTO → `RouteInfo` + `RouteType` 변환 |
| `GoogleDirectionsDto` | 신규 추가 | Google Directions API 응답 데이터 클래스 |
| `TrafficMonitorWorker` | 변경 없음 | `RouteRepository` 인터페이스만 사용하므로 내부 변경 없음 |
| `AlarmScheduler` | 변경 없음 | `RouteInfo` 도메인 모델만 사용하므로 내부 변경 없음 |

**핵심 원칙**: `RouteRepository` 인터페이스 계약을 유지하므로 UseCase 레이어 이상(ViewModel, Worker)은 변경 없음.

---

### 10.6 새벽 운행 중단 필터 — Google API 처리 여부 분석

#### 분석 결과

Google Maps Directions API는 `departure_time` 파라미터를 받지만, 새벽 운행 중단 시간대(00:30~05:30)에 대해 다음과 같이 동작한다:

- **운행 중단 시간대 요청 시**: 응답이 없거나(no routes), 당일 이른 아침 첫 차 시각으로 응답을 반환할 수 있음 — 앱이 기대하는 "해당 시간대 경로 없음" 에러를 명시적으로 반환하지 않음.
- **ODsay와 차이**: ODsay는 에러 코드 -8(-98) 반환 → FR-017 처리 가능. Google은 잘못된 경로를 정상 응답으로 반환할 수 있어 앱 레벨 필터가 필수.

#### 결론: FR-018 필터 로직 반드시 유지

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-018 (유지) | 새벽 00:30~05:30 시간대는 Google/ODsay API 호출 이전에 앱 레벨에서 먼저 대중교통 경로를 차단하고 WALK만 반환한다. Google API 전환 후에도 이 필터는 제거하지 않는다. | MUST |
| FR-067 | Google Directions API 응답에서 `departure_time`이 요청 시각과 1시간 이상 차이나는 경우(첫 차 시각 반환으로 간주), 해당 응답을 무효 처리하고 ODsay fallback을 시도한다. | MUST |

---

### 10.7 비용 관리 요구사항

| ID | 요구사항 | 우선순위 |
|----|----------|----------|
| FR-068 | Google Directions API 호출은 TrafficMonitorWorker의 3분 주기 재계산 외에는 발생하지 않도록 한다. (UI 탭 전환, 화면 회전 등으로 중복 호출 금지 — ViewModel 캐시 활용) | MUST |
| FR-069 | 동일 출발지·목적지·시각(±5분) 조건의 경로 응답은 Room DB에 5분간 캐시하여 중복 API 호출을 방지한다. | MUST |
| FR-070 | 월 Google API 호출 횟수를 앱 내 디버그 화면 또는 로그로 추적할 수 있어야 한다. (개발/테스트 목적) | SHOULD |

---

### 10.8 신규 요구사항 ID 요약

| ID | 분류 | 내용 | 우선순위 |
|----|------|------|----------|
| FR-060 | API 키 관리 | secrets-gradle-plugin + BuildConfig 주입 | MUST |
| FR-061 | API 키 관리 | local.properties gitignore + CI 환경변수 | MUST |
| FR-062 | API 키 보안 | Google Cloud 앱 제한 (패키지명 + SHA-1) | MUST |
| FR-063 | 비용 모니터링 | 월 크레딧 80% 알림 설정 | SHOULD |
| FR-064 | Fallback | Google 실패 시 ODsay 자동 전환 + UI 표시 | MUST |
| FR-065 | Fallback | quota 초과(429) 시 즉시 ODsay 전환, 다음 사이클 재시도 | MUST |
| FR-066 | Fallback | fallback 발생 시 로그 기록 | SHOULD |
| FR-067 | 운행 시간 필터 | Google 응답 departure_time 이상 감지 시 무효 처리 | MUST |
| FR-068 | 비용 관리 | 중복 호출 방지 (ViewModel 캐시) | MUST |
| FR-069 | 비용 관리 | 동일 조건 경로 5분 캐시 (Room DB) | MUST |
| FR-070 | 비용 모니터링 | 월 호출 횟수 추적 (디버그) | SHOULD |

---

*다음 단계: threat-modeler 투입 — Google Directions API 연동에 따른 DFD 업데이트 + STRIDE 재분석 (특히 API 키 노출, 비용 DoS 공격 위협 추가)*
